services:
  - type: web
    name: provision-brokerage-demo
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && python main_ui_server.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    staticPublishPath: frontend
